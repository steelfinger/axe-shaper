# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.zip
preview_*

# Local-only composed 3D viewer for `npm run dev` (production composes this
# from a pinned GitHub release into dist/viewer3d instead - see
# scripts/fetch-viewer3d.ts). Rebuild with the sibling repo's `npm run build`
# and copy its dist/ here to test the editor -> 3D viewer link locally.
public/viewer3d/

# Editor directories and files
.claude/
.agents/
skills-lock.json
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.html
!index.html
script_*.js

# Firebase CLI local cache
.firebase/
